"""Polish md with metaphors."""
from pathlib import Path
p = Path(r"C:\Users\Curry\Desktop\项目二\netops-assistant\docs\项目自述-从第一性原理到落地.md")
c = p.read_text(encoding="utf-8")

repls = [
    (
        "带我的主管每天的工作里有一块很耗时间：**在公司自研的网络管理系统上审批一大堆请求**。扩容、割接、故障处理、配置变更 —— 每条都要他登系统、看设备状态、对照手册、判断能不能批、批了之后还要跟进结果。一天下来几十条，很多是重复劳动：同一个 OSPF 邻居问题翻来覆去出现，同样的排障步骤他一天要敲好几遍。",
        "带我的主管每天的工作里有一块很耗时间：**在公司自研的网络管理系统上审批一大堆请求**。扩容、割接、故障处理、配置变更 —— 每条都要他登系统、看设备状态、对照手册、判断能不能批、批了之后还要跟进结果。一天下来几十条，很多是重复劳动：同一个 OSPF 邻居问题翻来覆去出现，同样的排障步骤他一天要敲好几遍。打个不太恰当但很形象的比方——他像一个被同样问题轰炸的客服主管，每个工单长得差不多，但每个都得自己重新查一遍。",
    ),
    (
        "这就是 ReAct（Reasoning + Acting）。我没有用 LangChain，因为我想自己把这个循环写一遍 —— 我得搞清楚它在什么地方会犯错，才知道怎么在审批场景里用得放心。",
        "这就是 ReAct（Reasoning + Acting）—— 推理和行动交替进行。说人话就是：像一个刚入职的新运维，遇到问题不会直接拍脑袋下结论，而是先在脑子里想一步（Reasoning），再动手查一个工具（Acting），看到结果再想下一步，直到证据够了才下结论。我没有用 LangChain，因为我想自己把这个循环写一遍 —— 我得搞清楚它在什么地方会犯错，才知道怎么在审批场景里用得放心。直接套别人的框架像坐出租车，知道终点在哪，但不知道司机为什么走这条路。",
    ),
    (
        "真实运维怎么解决？两条通道：\n\n\n* **业务通道**：走正常业务网（SSH）；\n\n* **带外通道**：走独立管理网（Console 口、ILO、docker exec）。\n\n我在代码里做成双通道：netmiko 走 SSH，docker exec 走带外，互相 fallback。",
        "真实运维怎么解决？就像机房里的服务器要双路电源——主电源挂了，UPS 还能撑着让你关机。网络设备也一样：\n\n* **业务通道**：走正常业务网（SSH）—— 你平时办公走的那条路；\n* **带外通道**：走独立管理网（Console 口、ILO、docker exec）—— 那条专门留给管理员的“后门”，和业务流量物理隔离。\n\n我在代码里做成双通道：netmiko 走 SSH，docker exec 走带外，互相 fallback。业务通道断了，带外还能让你登进去把故障收回来。",
    ),
    (
        "我加了一个 `dry_run` 动作：\n\n\n1. Agent 说要注入故障时，系统先返回 “将要执行什么命令、影响哪个接口、预计什么后果”；\n\n2. 60 秒内必须有一次 `dry_run` 请求过这个组合；\n\n3. 没预览过就直接 inject，**直接拒绝执行**。\n\n这个逻辑写在 `fault_state.py` 里。它的核心思想很朴素：**让模型在动手前先说一遍要做什么，人确认过了再干。** 这就是主管审批流程的缩影。",
        "我加了一个 `dry_run` 动作，思路直接搬自医院手术室那张“手术安全核对表”—— 医生动刀前必须停下来念一遍：患者是谁、做什么手术、切哪一侧。少念一遍不准下刀：\n\n1. Agent 说要注入故障时，系统先返回“将要执行什么命令、影响哪个接口、预计什么后果、逆操作是什么”；\n2. 60 秒内必须有一次 `dry_run` 请求过这个组合；\n3. 没预览过就直接 inject，**直接拒绝执行**。\n\n这个逻辑写在 `fault_state.py` 里。核心思想很朴素：**让模型在动手前先说一遍要做什么，人确认过了再干。** 这就是主管审批流程的缩影——只不过主管是肉眼核对，我是代码层强校验。模型想跳过这一步？代码不让。",
    ),
    (
        "第一版 `fault_state.json` 直接读→改→写。两个请求同时进来，后写的把先写的覆盖了。",
        "第一版 `fault_state.json` 直接读→改→写。这就像两个人同时改同一份 Excel：你读到一半，我也读到一半，我们都改完保存，你改的那几行直接被我覆盖了。两个请求同时进来，后写的把先写的覆盖了。",
    ),
    (
        "**修法**：atomic write—— 先写 `.tmp`，写完再 `os.replace`。`os.replace` 是原子操作：要么旧文件在，要么新文件在，不会有中间状态。",
        "**修法**：atomic write—— 先写一个 `.tmp` 临时文件，写完再 `os.replace` 原子替换。`os.replace` 这个系统调用在操作系统层面保证：要么旧文件在，要么新文件在，不会有“写到一半”的中间状态。就像换招牌：你先把新招牌在后台做好，再一次性挂上去，路过的人永远不会看到半个旧招牌半个新招牌。",
    ),
]

n = 0
for old, new in repls:
    if old in c:
        c = c.replace(old, new)
        n += 1
    else:
        print(f"NOT FOUND: {old[:40]}...")

p.write_text(c, encoding="utf-8")
print(f"OK, replaced {n}/{len(repls)}")
