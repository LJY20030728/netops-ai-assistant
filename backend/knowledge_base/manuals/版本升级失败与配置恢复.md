# 版本升级失败与配置恢复

## 典型症状
升级后设备起不来；新版本与现网配置不兼容；升级中途断电。

## 原理速记
升级分下载→激活→重启→确认；需检查版本兼容性矩阵、空间充足、双分区倒换；失败需回退旧版本。

## 排查步骤
- `display version` —— 确认当前版本与目标版本兼容
- `dir flash:` —— 确认空间够放新镜像
- `display startup` —— 看下次启动分区与镜像

## 常见根因
镜像校验失败；空间不足；新版本命令弃用致启动配置中断。

## 处置与验证
升级后版本正确；业务无中断；失败时能从备份分区回退。
