# 网络割接后回退操作规范

## 典型症状
割接后业务异常，需要快速回退；但回退步骤不清晰致二次故障。

## 原理速记
割接=变更，回退预案必须先于变更准备：配置备份、revert 脚本、监控基线；回退按时间线逆序。

## 排查步骤
- `割接前 save 配置并导出` —— 建立回退点
- `割接后逐项冒烟测试` —— 按业务清单验证
- `异常时按预案 reverse` —— 先回网络再回应用层

## 常见根因
无回退预案；回退顺序错（先回应用层但网络已断）；监控基线缺失。

## 处置与验证
割接有备份有预案；冒烟全过；回退可在 10 分钟内恢复旧状态。
